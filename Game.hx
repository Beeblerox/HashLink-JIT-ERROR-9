class Game extends hxd.App
{
    static function main()
    {
        var array:issue.Array<Dynamic> = [];
        if (array[0] != null)
        {
            trace(array[0].length);
        }

        trace("Success!!!");
    }
}